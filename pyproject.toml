[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = [
    "-q",
    "-p",
    "no:cacheprovider",
    "--cov=src/portfolio_analytics",
    "--cov-report=term-missing",
    "--cov-report=xml",
    "--cov-fail-under=20",
]
